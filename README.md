# python-response-code

A super basic python flask app listening on TCP/80 to respond with a defined HTTP response code.

An environment variable `CODE` set with an integer will set the response code, default is `200`.
